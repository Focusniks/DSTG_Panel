# Панель управления ботами Discord и Telegram

Современная веб-панель для управления ботами Discord и Telegram с полным контролем над процессами, файлами, базами данных и Git репозиториями.

## 🎯 Основные возможности

- ✅ **Создание и управление ботами** - Discord и Telegram боты в одном интерфейсе
- ✅ **Мониторинг в реальном времени** - CPU, RAM, PID, статус работы
- ✅ **Редактор кода** - полнофункциональный редактор с подсветкой синтаксиса
- ✅ **Управление файлами** - создание, редактирование, удаление, загрузка файлов
- ✅ **Просмотр медиа** - изображения, видео и аудио прямо в браузере
- ✅ **SQLite редактор** - полноценный редактор баз данных с работой с таблицами, строками и столбцами
- ✅ **Git интеграция** - клонирование и обновление репозиториев из GitHub
- ✅ **SSH ключи** - поддержка приватных репозиториев
- ✅ **Автозапуск** - автоматический запуск ботов при старте панели
- ✅ **Лимиты ресурсов** - контроль CPU и памяти для каждого бота
- ✅ **Системная интеграция** - автозапуск через systemd

## 📋 Системные требования

### Минимальные требования:
- **ОС**: Linux (Ubuntu 20.04+, Debian 10+, CentOS 7+, Fedora 30+)
- **CPU**: 1 ядро
- **RAM**: 512 MB
- **Диск**: 5 GB свободного места
- **Python**: 3.8 или выше

### Рекомендуемые требования:
- **CPU**: 2+ ядра
- **RAM**: 2+ GB
- **Диск**: 20+ GB SSD
- **Python**: 3.10+

## 🚀 Быстрая установка на VDS

### Автоматическая установка (рекомендуется)

1. **Подключитесь к серверу по SSH:**
```bash
ssh root@ваш_IP_адрес
```

2. **Клонируйте репозиторий или загрузите файлы:**
```bash
cd ~
git clone https://github.com/ваш_username/DSTG_Panel.git
cd DSTG_Panel
```

3. **Запустите скрипт установки:**
```bash
chmod +x install_panel.sh
sudo ./install_panel.sh
```

Скрипт автоматически выполнит:
- ✅ Создание системного пользователя `bot-panel`
- ✅ Установку всех необходимых зависимостей
- ✅ Создание виртуального окружения Python
- ✅ Установку Python пакетов
- ✅ Настройку systemd service для автозапуска
- ✅ Создание необходимых директорий

4. **Запустите панель:**
```bash
sudo systemctl start bot-panel
sudo systemctl status bot-panel
```

5. **Откройте в браузере:**
```
http://ваш_IP_адрес:8000
```

**Пароль по умолчанию:** `admin` (обязательно смените после первого входа!)

## 🔧 Управление панелью

### Команды systemd

```bash
# Запуск
sudo systemctl start bot-panel

# Остановка
sudo systemctl stop bot-panel

# Перезапуск
sudo systemctl restart bot-panel

# Статус
sudo systemctl status bot-panel

# Просмотр логов
sudo journalctl -u bot-panel -f

# Просмотр последних 100 строк
sudo journalctl -u bot-panel -n 100

# Включить автозапуск
sudo systemctl enable bot-panel

# Отключить автозапуск
sudo systemctl disable bot-panel
```

### Настройка файрвола

**Ubuntu/Debian (UFW):**
```bash
sudo ufw allow 8000/tcp
sudo ufw reload
```

**CentOS/RHEL/Fedora (firewalld):**
```bash
sudo firewall-cmd --permanent --add-port=8000/tcp
sudo firewall-cmd --reload
```

## 📖 Использование

### Создание бота

1. Войдите в панель (http://ваш_IP:8000)
2. Нажмите "Создать бота"
3. Заполните информацию:
   - Название бота
   - Тип (Discord или Telegram)
   - Стартовый файл (например, `main.py`)
   - Лимиты ресурсов (CPU %, память MB)
   - URL GitHub репозитория (опционально)
4. Сохраните настройки

### Редактирование кода

1. Откройте бота для редактирования (кнопка "Управление")
2. Перейдите во вкладку "Файлы"
3. Используйте файловое дерево для навигации
4. Редактируйте файлы в редакторе кода
5. Сохраняйте изменения кнопкой "Сохранить"

### Работа с базами данных (SQLite)

1. В настройках бота перейдите во вкладку "База данных"
2. Нажмите "Создать базу данных"
3. Используйте встроенный SQL редактор для выполнения запросов
4. Откройте SQLite редактор для полноценной работы с таблицами, строками и столбцами

### Работа с GitHub

1. При создании/редактировании бота укажите URL GitHub репозитория
2. Используйте SSH URL для приватных репозиториев: `git@github.com:username/repo.git`
3. Настройте SSH ключ в настройках панели (Settings → SSH Key)
4. Добавьте публичный ключ в GitHub (Settings → SSH and GPG keys)
5. Для приватных репозиториев добавьте ключ как Deploy Key
6. Используйте кнопку "Обновить из GitHub" для синхронизации

### Автозапуск ботов

1. Откройте настройки бота
2. Включите опцию "Автозапуск при старте панели"
3. Бот будет автоматически запускаться при каждом запуске панели

## 📂 Структура проекта

```
DSTG_Panel/
├── backend/              # Backend код (FastAPI)
│   ├── auth.py          # Система авторизации
│   ├── bot_manager.py   # Управление процессами ботов
│   ├── config.py        # Конфигурация
│   ├── database.py     # Работа с SQLite БД панели
│   ├── git_manager.py   # Работа с Git
│   ├── main.py          # Главный файл FastAPI приложения
│   ├── sqlite_manager.py # Управление SQLite БД ботов
│   └── ssh_manager.py   # Управление SSH ключами
├── frontend/            # Frontend (HTML, CSS, JS)
│   ├── static/          # Статические файлы
│   │   ├── css/        # Стили
│   │   ├── js/         # JavaScript
│   │   └── favicon.svg  # Иконка сайта
│   └── templates/       # HTML шаблоны
├── bots/                # Хранилище ботов (создается автоматически)
├── data/                # Данные панели
│   ├── panel.db        # SQLite база данных панели
│   ├── admin_password.hash  # Хеш пароля администратора
│   └── ssh/            # SSH ключи для Git
├── start_panel.py       # Скрипт запуска панели
├── install_panel.sh     # Скрипт автоматической установки
├── requirements.txt     # Python зависимости
└── README.md           # Документация
```

## ⚙️ Конфигурация

Основные настройки находятся в `backend/config.py`:

- **Пароль администратора**: Можно изменить через веб-интерфейс в разделе "Настройки"
- **Хост и порт панели**: По умолчанию `0.0.0.0:8000`
- **Лимиты ресурсов**: Настраиваются для каждого бота индивидуально

## 🔐 Безопасность

⚠️ **Важные рекомендации:**

1. **Смените пароль администратора** сразу после установки
2. **Используйте HTTPS** в продакшене (через Nginx reverse proxy)
3. **Настройте файрвол** - открывайте только необходимые порты
4. **Регулярно обновляйте систему и зависимости**
5. **Ограничьте доступ к панели** через файрвол или Nginx
6. **Регулярно делайте резервные копии** директории `/opt/bot-panel/data`

## 🐛 Решение проблем

### Панель не запускается

```bash
# Проверьте логи
sudo journalctl -u bot-panel -n 50

# Проверьте статус
sudo systemctl status bot-panel

# Проверьте права доступа
sudo chown -R bot-panel:bot-panel /opt/bot-panel
```

### Порт 8000 уже занят

Измените порт в `/opt/bot-panel/backend/config.py`:
```python
PANEL_PORT = 8001  # или другой свободный порт
```

Затем перезапустите:
```bash
sudo systemctl restart bot-panel
```

### Боты не запускаются

1. Проверьте логи бота в панели (вкладка "Логи")
2. Убедитесь, что все зависимости установлены
3. Проверьте, что стартовый файл указан правильно
4. Проверьте права доступа к файлам бота

### Обновление панели

```bash
cd /opt/bot-panel
sudo -u bot-panel git pull origin main
sudo -u bot-panel /opt/bot-panel/venv/bin/pip install -r requirements.txt
sudo systemctl restart bot-panel
```

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи: `sudo journalctl -u bot-panel -f`
2. Проверьте логи ботов в веб-интерфейсе
3. Убедитесь, что все системные требования выполнены
4. Проверьте конфигурацию в `backend/config.py`
