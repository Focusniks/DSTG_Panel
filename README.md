# Панель управления ботами Discord и Telegram

Веб-панель для управления ботами Discord и Telegram с возможностью создания, редактирования, запуска и мониторинга ботов на VDS сервере.

## 🎯 Возможности

- ✅ Создание и управление ботами Discord и Telegram
- ✅ Запуск и остановка ботов через веб-интерфейс
- ✅ Мониторинг использования ресурсов (CPU, RAM) в реальном времени
- ✅ Редактор кода с подсветкой синтаксиса (CodeMirror)
- ✅ Управление файлами ботов (создание, редактирование, удаление, загрузка)
- ✅ Просмотр изображений, видео и аудио файлов прямо в редакторе
- ✅ Управление MySQL базами данных для ботов
- ✅ SQL редактор для работы с БД
- ✅ Интеграция с phpMyAdmin (автологин)
- ✅ Настройка лимитов ресурсов для каждого бота
- ✅ Автозапуск панели и ботов при перезагрузке системы (systemd)
- ✅ Интеграция с GitHub (клонирование и обновление репозиториев)
- ✅ Поддержка приватных GitHub репозиториев через SSH ключи
- ✅ Смена пароля администратора через веб-интерфейс
- ✅ Автоматическая установка зависимостей для ботов

## 📋 Системные требования

### Минимальные требования:
- **ОС**: Linux (Ubuntu 20.04+, Debian 10+, CentOS 7+, Fedora 30+)
- **CPU**: 1 ядро
- **RAM**: 512 MB (рекомендуется 1 GB)
- **Диск**: 5 GB свободного места
- **Python**: 3.8 или выше
- **MySQL**: 5.7+ или MariaDB 10.3+ (опционально, для создания БД ботов)

### Рекомендуемые требования:
- **CPU**: 2+ ядра
- **RAM**: 2+ GB
- **Диск**: 20+ GB SSD
- **Python**: 3.10+
- **MySQL**: 8.0+ или MariaDB 10.6+

### Дополнительное ПО:
- `git` - для работы с GitHub репозиториями
- `openssh-client` - для SSH ключей
- `mysql-client` - для работы с MySQL (опционально)
- `phpMyAdmin` - для веб-интерфейса управления MySQL (опционально, см. раздел "Установка phpMyAdmin")

## 🚀 Установка на VDS

### Шаг 1: Подготовка сервера

1. Подключитесь к вашему VDS серверу по SSH:
```bash
ssh root@ваш_IP_адрес
```

2. Обновите систему:
```bash
# Для Ubuntu/Debian:
apt update && apt upgrade -y

# Для CentOS/RHEL:
yum update -y

# Для Fedora:
dnf update -y
```

### Шаг 2: Клонирование репозитория

```bash
# Перейдите в домашнюю директорию
cd ~

# Клонируйте репозиторий (замените на ваш репозиторий)
git clone https://github.com/ваш_username/ds_tg_panel_bot.git
cd ds_tg_panel_bot
```

Если у вас нет Git репозитория, просто загрузите файлы на сервер через SFTP/SCP.

### Шаг 3: Установка через скрипт (рекомендуется)

```bash
# Сделайте скрипт исполняемым
chmod +x install_panel.sh

# Запустите установку (требуются права root)
sudo ./install_panel.sh
```

Скрипт автоматически:
- Создаст системного пользователя `bot-panel`
- Установит все необходимые зависимости
- Создаст виртуальное окружение Python
- Установит Python пакеты
- Настроит systemd service для автозапуска

### Шаг 4: Ручная установка (альтернативный способ)

Если предпочитаете установить вручную:

#### 4.1. Создание системного пользователя

```bash
sudo useradd -r -s /bin/bash -d /opt/bot-panel -m bot-panel
```

#### 4.2. Копирование файлов

```bash
sudo cp -r ~/ds_tg_panel_bot /opt/bot-panel
sudo chown -R bot-panel:bot-panel /opt/bot-panel
```

#### 4.3. Установка системных зависимостей

```bash
# Ubuntu/Debian:
sudo apt-get update
sudo apt-get install -y python3-pip python3-venv git mysql-client openssh-client

# CentOS/RHEL:
sudo yum install -y python3-pip python3-devel git mysql openssh-clients

# Fedora:
sudo dnf install -y python3-pip python3-devel git mysql openssh-clients
```

#### 4.4. Создание виртуального окружения

```bash
sudo -u bot-panel python3 -m venv /opt/bot-panel/venv
sudo -u bot-panel /opt/bot-panel/venv/bin/pip install --upgrade pip
sudo -u bot-panel /opt/bot-panel/venv/bin/pip install -r /opt/bot-panel/requirements.txt
```

#### 4.5. Создание systemd service

Создайте файл `/etc/systemd/system/bot-panel.service`:

```ini
[Unit]
Description=Bot Panel - Discord and Telegram Bot Management Panel
After=network.target mysql.service

[Service]
Type=simple
User=bot-panel
WorkingDirectory=/opt/bot-panel
Environment="PATH=/opt/bot-panel/venv/bin"
ExecStart=/opt/bot-panel/venv/bin/python /opt/bot-panel/start_panel.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

Затем выполните:

```bash
sudo systemctl daemon-reload
sudo systemctl enable bot-panel.service
```

### Шаг 5: Настройка конфигурации

Отредактируйте файл конфигурации:

```bash
sudo nano /opt/bot-panel/backend/config.py
```

Основные настройки:

- **Пароль администратора**: Можно изменить после первого входа через веб-интерфейс, или сгенерировать хеш:
  ```bash
  sudo -u bot-panel /opt/bot-panel/venv/bin/python -c "import bcrypt; print(bcrypt.hashpw('ваш_пароль'.encode(), bcrypt.gensalt()).decode())"
  ```
  Затем установите переменную окружения `ADMIN_PASSWORD_HASH` или измените в `config.py`.

- **MySQL настройки** (если используете):
  ```python
  MYSQL_HOST = "localhost"
  MYSQL_PORT = 3306
  MYSQL_ROOT_USER = "root"
  MYSQL_ROOT_PASSWORD = "ваш_пароль_mysql"
  ```

- **Хост и порт панели**:
  ```python
  PANEL_HOST = "0.0.0.0"  # Слушать на всех интерфейсах
  PANEL_PORT = 8000       # Порт панели
  ```

### Шаг 6: Настройка файрвола

Если у вас установлен UFW (Ubuntu) или firewalld (CentOS):

```bash
# Ubuntu/Debian (UFW):
sudo ufw allow 8000/tcp

# CentOS/RHEL/Fedora (firewalld):
sudo firewall-cmd --permanent --add-port=8000/tcp
sudo firewall-cmd --reload
```

### Шаг 7: Запуск панели

```bash
# Запуск service
sudo systemctl start bot-panel

# Проверка статуса
sudo systemctl status bot-panel

# Просмотр логов
sudo journalctl -u bot-panel -f
```

Панель автоматически запустится при загрузке системы.

### Шаг 8: Доступ к панели

Откройте в браузере:
```
http://ваш_IP_адрес:8000
```

Или если настроили домен:
```
http://ваш_домен:8000
```

**Пароль по умолчанию**: `admin` (обязательно смените после первого входа!)

## 🔧 Управление service

```bash
# Запуск
sudo systemctl start bot-panel

# Остановка
sudo systemctl stop bot-panel

# Перезапуск
sudo systemctl restart bot-panel

# Проверка статуса
sudo systemctl status bot-panel

# Просмотр логов
sudo journalctl -u bot-panel -f

# Просмотр последних 100 строк логов
sudo journalctl -u bot-panel -n 100

# Отключение автозапуска
sudo systemctl disable bot-panel

# Включение автозапуска
sudo systemctl enable bot-panel
```

## 📦 Настройка HTTPS (рекомендуется)

Для продакшена настоятельно рекомендуется использовать HTTPS через reverse proxy (Nginx).

### Установка Nginx

```bash
# Ubuntu/Debian:
sudo apt-get install -y nginx

# CentOS/RHEL:
sudo yum install -y nginx
```

### Настройка Nginx

Создайте файл `/etc/nginx/sites-available/bot-panel`:

```nginx
server {
    listen 80;
    server_name ваш_домен.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Активируйте конфигурацию:

```bash
# Ubuntu/Debian:
sudo ln -s /etc/nginx/sites-available/bot-panel /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# CentOS/RHEL:
sudo cp /etc/nginx/sites-available/bot-panel /etc/nginx/conf.d/bot-panel.conf
sudo nginx -t
sudo systemctl restart nginx
```

### Настройка SSL через Let's Encrypt

```bash
# Установка Certbot
sudo apt-get install -y certbot python3-certbot-nginx  # Ubuntu/Debian
# или
sudo yum install -y certbot python3-certbot-nginx      # CentOS/RHEL

# Получение сертификата
sudo certbot --nginx -d ваш_домен.com

# Автоматическое обновление (уже настроено через cron)
```

## 🔐 Безопасность

⚠️ **Важные рекомендации по безопасности:**

1. **Смените пароль администратора** сразу после установки через веб-интерфейс
2. **Используйте HTTPS** в продакшене (см. раздел выше)
3. **Настройте файрвол** - открывайте только необходимые порты
4. **Регулярно обновляйте систему и зависимости**:
   ```bash
   sudo apt update && sudo apt upgrade -y  # Ubuntu/Debian
   sudo -u bot-panel /opt/bot-panel/venv/bin/pip install --upgrade -r /opt/bot-panel/requirements.txt
   ```
5. **Ограничьте доступ к панели** через файрвол или Nginx (allow/deny директивы)
6. **Используйте сильные пароли** для MySQL и SSH
7. **Регулярно делайте резервные копии** директории `/opt/bot-panel/data`

## 📝 Использование

### Создание бота

1. Войдите в панель (http://ваш_IP:8000)
2. Нажмите "Создать бота"
3. Заполните информацию:
   - Название бота
   - Тип (Discord или Telegram)
   - Стартовый файл (например, `main.py`)
   - Лимиты ресурсов (CPU %, память MB)
   - URL GitHub репозитория (опционально)
4. Сохраните настройки

### Редактирование кода

1. Откройте бота для редактирования (кнопка "Управление")
2. Перейдите во вкладку "Файлы"
3. Используйте файловое дерево для навигации
4. Редактируйте файлы в редакторе кода
5. Сохраняйте изменения кнопкой "Сохранить"

### Просмотр медиа-файлов

Панель автоматически определяет и отображает:
- **Изображения**: JPG, PNG, GIF, WebP, SVG, BMP, TIFF, AVIF, HEIC, APNG, ICO, JPEG XL
- **Видео**: MP4, WebM, OGG, MOV, AVI, MKV, FLV, WMV, M4V, MPEG, 3GP и другие
- **Аудио**: MP3, WAV, OGG, FLAC, M4A, AAC, WMA, Opus и другие

Просто выберите файл в файловом дереве - он автоматически отобразится в соответствующем плеере.

### Создание базы данных

1. В настройках бота перейдите во вкладку "База данных"
2. Нажмите "Создать базу данных"
3. Используйте встроенный SQL редактор для работы с БД
4. Или откройте phpMyAdmin с автологином

### Установка phpMyAdmin (опционально)

phpMyAdmin предоставляет удобный веб-интерфейс для управления MySQL базами данных. Если phpMyAdmin не установлен, при переходе по ссылке будет показана инструкция по установке.

#### Установка на Ubuntu/Debian:

```bash
# Обновление списка пакетов
sudo apt update

# Установка Apache и PHP (если еще не установлены)
sudo apt install -y apache2 php php-mysql php-mbstring php-zip php-gd php-json php-curl

# Установка phpMyAdmin
sudo apt install -y phpmyadmin

# Во время установки выберите:
# - Веб-сервер: apache2
# - Настроить базу данных: Да
# - Пароль для phpmyadmin: (укажите пароль)
```

После установки создайте символическую ссылку:

```bash
# Создание ссылки для доступа через панель
sudo ln -s /usr/share/phpmyadmin /var/www/html/phpMyAdmin

# Или если используете другой путь:
sudo ln -s /usr/share/phpmyadmin /var/www/html/phpmyadmin
```

#### Альтернативный вариант: Установка через Docker

Если у вас установлен Docker, можно использовать контейнер:

```bash
docker run -d \
  --name phpmyadmin \
  -e PMA_HOST=localhost \
  -e PMA_PORT=3306 \
  -p 8080:80 \
  --restart=always \
  phpmyadmin/phpmyadmin
```

После этого phpMyAdmin будет доступен по адресу: `http://ваш_ip:8080`

**Важно:** После установки phpMyAdmin перезапустите панель управления ботами, чтобы изменения вступили в силу:

```bash
sudo systemctl restart bot-panel
```

### Работа с GitHub

1. При создании/редактировании бота укажите URL GitHub репозитория
2. Используйте SSH URL для приватных репозиториев: `git@github.com:username/repo.git`
3. Настройте SSH ключ в настройках панели (Settings → SSH Key)
4. Добавьте публичный ключ в GitHub (Settings → SSH and GPG keys)
5. Для приватных репозиториев добавьте ключ как Deploy Key (Settings → Deploy keys)
6. Используйте кнопку "Обновить из GitHub" для синхронизации

### Запуск и остановка ботов

1. На главной странице нажмите "Запустить" у нужного бота
2. Отслеживайте статус и использование ресурсов в реальном времени
3. Используйте "Остановить" для остановки бота
4. Боты автоматически запустятся при перезагрузке сервера, если были запущены ранее

## 📂 Структура проекта

```
ds_tg_panel_bot/
├── backend/              # Backend код (FastAPI)
│   ├── auth.py          # Система авторизации
│   ├── bot_manager.py   # Управление процессами ботов
│   ├── config.py        # Конфигурация
│   ├── database.py      # Работа с SQLite
│   ├── db_manager.py    # Управление MySQL БД
│   ├── git_manager.py   # Работа с Git
│   ├── main.py          # Главный файл FastAPI приложения
│   └── ssh_manager.py   # Управление SSH ключами
├── frontend/            # Frontend (HTML, CSS, JS)
│   ├── static/          # Статические файлы
│   │   ├── css/        # Стили
│   │   └── js/         # JavaScript
│   └── templates/       # HTML шаблоны
├── bots/                # Хранилище ботов (создается автоматически)
├── data/                # Данные панели
│   ├── panel.db        # SQLite база данных
│   ├── admin_password.hash  # Хеш пароля администратора
│   └── ssh/            # SSH ключи для Git
├── start_panel.py       # Скрипт запуска панели
├── install_panel.sh     # Скрипт установки на Linux
├── requirements.txt     # Python зависимости
└── README.md           # Документация
```

## ⚙️ Конфигурация через переменные окружения

Вы можете настроить панель через переменные окружения, создав файл `/opt/bot-panel/.env` или настроив их в systemd service:

```bash
# Редактируйте service файл
sudo nano /etc/systemd/system/bot-panel.service

# Добавьте в секцию [Service]:
Environment="ADMIN_PASSWORD_HASH=ваш_хеш"
Environment="MYSQL_HOST=localhost"
Environment="MYSQL_ROOT_PASSWORD=ваш_пароль"
Environment="PANEL_PORT=8000"

# Перезагрузите service
sudo systemctl daemon-reload
sudo systemctl restart bot-panel
```

## 🐛 Решение проблем

### Панель не запускается

```bash
# Проверьте логи
sudo journalctl -u bot-panel -n 50

# Проверьте статус
sudo systemctl status bot-panel

# Проверьте права доступа
sudo chown -R bot-panel:bot-panel /opt/bot-panel
```

### Ошибки с MySQL

Убедитесь, что MySQL/MariaDB запущен и доступен:
```bash
sudo systemctl status mysql  # или mariadb
mysql -u root -p -e "SELECT 1"
```

### Порт 8000 уже занят

Измените порт в `/opt/bot-panel/backend/config.py`:
```python
PANEL_PORT = 8001  # или другой свободный порт
```

### Боты не запускаются

1. Проверьте логи бота в панели (вкладка "Логи")
2. Убедитесь, что все зависимости установлены (requirements.txt)
3. Проверьте, что стартовый файл указан правильно

### Обновление панели

```bash
cd /opt/bot-panel
sudo -u bot-panel git pull origin main
sudo -u bot-panel /opt/bot-panel/venv/bin/pip install -r requirements.txt
sudo systemctl restart bot-panel
```

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи: `sudo journalctl -u bot-panel -f`
2. Проверьте логи ботов в веб-интерфейсе
3. Убедитесь, что все системные требования выполнены
4. Проверьте конфигурацию в `backend/config.py`
